package com.realestate.app.ui

/** Update this when the real online store address is available. */
const val STORE_URL = "https://example.com/store"

// Placeholders, same pattern as STORE_URL above — swap in the business's real support channels
// when they exist. Kept as plain constants (not fetched from anywhere) so the Help Center works
// correctly the moment real values are filled in, with no other code needing to change.
const val SUPPORT_EMAIL = "support@example.com"
const val SUPPORT_TELEGRAM_URL = "https://t.me/example_support"
const val SUPPORT_WHATSAPP_URL = "https://wa.me/10000000000"
const val SUPPORT_WEBSITE_URL = "https://example.com/help"
